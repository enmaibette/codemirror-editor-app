- Store owner and balance as instance attributes: `self.owner = owner` and `self.balance = balance`.
- In `withdraw`, check `self.balance >= amount` before subtracting.
- `get_balance()` should return `self.balance`.

