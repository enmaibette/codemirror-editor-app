- To inherit from a parent class, pass it in parentheses: `class Dog(Animal):`.
- Overriding means defining a method with the same name in the child class.
- You can call `super().speak()` in a child method if you need parent behavior first.

